# Development / testing dependencies.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# or, into an editable install: pip install -e ".[dev]"
-r requirements.txt

pytest>=8
pytest-cov>=5
ruff>=0.6